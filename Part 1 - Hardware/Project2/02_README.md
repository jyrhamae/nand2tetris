# Project 2 — Boolean Arithmetic

## Overview
1–2 sentences on what this project is and what it produces.  
Example: *Implements the Boolean logic chips that form the foundation of the Hack computer, built entirely from NAND gates.*

---

## Chips / Components Built
| Name | Description |
|------|-------------|
| `And.hdl` | Outputs 1 only if both inputs are 1 |
| `Or.hdl`  | Outputs 1 if either input is 1 |
| `Mux.hdl` | Returns a or b based on selector bit |

*(For software projects like the assembler or compiler, swap this for a "Modules" or "Files" table)*

---

## How It Works
3–5 sentences explaining your approach or any interesting implementation details.  
Avoid just restating what the chip does — explain *how* you built it or any decisions you made.

---

## How to Run
```bash
# Example for HDL projects
# Load the .hdl file in the Hardware Simulator and run the provided .tst script

# Example for software projects
python assembler.py Add.asm
```

---

## Key Concepts
- Concept one (e.g. multiplexing)
- Concept two (e.g. two's complement)
- Concept three

---

## Challenges
One short paragraph on anything that tripped you up or required extra thought.  
This is optional but makes your README feel genuine and shows self-awareness.
